spring:
  datasource:
    url: jdbc:mysql://expensewise-db.cz0828qeygry.eu-north-1.rds.amazonaws.com:3306/product_db
    username: admin
    password: Karthik1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

server:
  port: 8089